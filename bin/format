#!/usr/bin/env ruby
require "json"

begin
  require "zig"
rescue LoadError
  $:.unshift File.expand_path('../lib', File.dirname(__FILE__))
  require "zig"
end

data = JSON.parse(File.read(ARGV[0]))
ZIG.print(data)
